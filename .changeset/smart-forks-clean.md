---
"create-cf-token": patch
---

Refresh the `better-result` dependency to `^2.8.1` and update related lockfile entries.

This also includes the current dev dependency refresh for `lefthook` and `@typescript/native-preview`.
