# Implementation Plans

Generated by the improve skill on 2026-06-25. Execute in the order below unless dependencies say otherwise. Each executor: read the plan fully before starting, honor its STOP conditions, and update your row when done.

## Execution order & status

| Plan | Title                                                     | Priority | Effort | Depends on | Status |
| ---- | --------------------------------------------------------- | -------- | ------ | ---------- | ------ |
| 001  | Stop no-progress restricted-permission retries            | P1       | S      | -          | DONE   |
| 002  | Move Clack cancellation detection behind prompts boundary | P2       | S      | -          | TODO   |
| 003  | Add package-surface tests for automation skill assets     | P2       | S      | -          | TODO   |
| 004  | Refresh contributor docs after source restructure         | P3       | S      | -          | DONE   |

Status values: TODO | IN PROGRESS | DONE | BLOCKED (with one-line reason) | REJECTED (with one-line rationale — finding fixed independently or approach abandoned)

## Dependency notes

- The plans are independent. Plan 001 is highest priority because it prevents repeated API create attempts in a user-facing failure mode.
- Plan 002 should land before future prompt/orchestrator work so the documented prompt isolation rule remains enforceable by example.
- Plan 003 is a regression guard; it is easiest to land before changing package `files`, automation assets, or `src/automation/paths.ts`.

## Findings considered and rejected

- `CF_API_BASE_URL` can redirect API requests: rejected as a security finding because the repository intentionally documents this test/development override in `AGENTS.md`, and honoring environment-controlled service endpoints is by-design for this CLI.
- Created token values are printed to stdout in interactive and automation flows: rejected as a defect because showing the one-time Cloudflare token value is the core product behavior documented in `README.md`; any hardening should be opt-in UX work, not a bug fix.
- Recursive pagination in `src/api/client.ts`: rejected for now because account and permission-group page counts are expected to be low; there is no evidence of a reachable stack-depth or performance issue in normal use.
